{
  "name": "@deep-foundation/capacitor-motion",
  "keywords": [
    "deep-package"
  ],
  "version": "0.0.7",
  "scripts": {
    "npm-pull": "(git diff --quiet || (echo 'You have unstaged changes. Stash them (git stash) or commit them (git commit)'; exit 1)) && npm install ${npm_package_name}@latest --no-save && mv --force node_modules/${npm_package_name}/deep.json ./",
    "build": "tsc"
  },
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "files": [
    "/dist",
    "./deep.json"
  ],
  "dependencies": {
    "@deep-foundation/core": "^0.0.2",
    "@freephoenix888/object-to-links-async-converter": "^0.0.10",
    "@deep-foundation/capacitor-device": "^1.0.0"
  }
}